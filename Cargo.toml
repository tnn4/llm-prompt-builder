[package]
name = "prompt-builder"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
inquire = "0.6"
rand = "0.10.1"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
